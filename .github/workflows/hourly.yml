name: Sponsor Watch

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Restore dedupe state
        id: cache-restore
        uses: actions/cache/restore@v4
        with:
          path: state/sponsor_watch.sqlite3
          key: sponsor-watch-state-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            sponsor-watch-state-${{ github.ref_name }}-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write runtime config
        run: |
          mkdir -p state
          cat > config.yaml <<EOF
          roam:
            base_url: https://api.ro.am/v0
            token: ${ROAM_API_TOKEN}
            channels:
              vendors: ${ROAM_VENDOR_CHANNEL}
              lenders: ${ROAM_LENDER_CHANNEL}
          state:
            path: state/sponsor_watch.sqlite3
          request_timeout_seconds: 20
          request_retries: 2
          max_items_per_company: 3
          google_news:
            hl: en-US
            gl: US
            ceid: US:en
          EOF
        env:
          ROAM_API_TOKEN: ${{ secrets.ROAM_API_TOKEN }}
          ROAM_VENDOR_CHANNEL: ${{ secrets.ROAM_VENDOR_CHANNEL }}
          ROAM_LENDER_CHANNEL: ${{ secrets.ROAM_LENDER_CHANNEL }}

      - name: Run watcher
        run: python sponsor_watch.py --config config.yaml --companies companies.yaml run

      - name: Save dedupe state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: state/sponsor_watch.sqlite3
          key: sponsor-watch-state-${{ github.ref_name }}-${{ github.run_id }}
